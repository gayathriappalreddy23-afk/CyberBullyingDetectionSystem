from django.urls import path
from . import views

app_name = 'prediction'

urlpatterns = [
    # TODO: Add prediction/detection URLs
    # path('detect/', views.detect_view, name='detect'),
    # path('result/<int:pk>/', views.result_view, name='result'),
]
